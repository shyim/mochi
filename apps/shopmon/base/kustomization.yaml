apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# App workload shared by all environments. Namespace, image tag, config,
# secrets, database, and routes are supplied by the overlays.
resources:
  - api.yaml
  - redis.yaml
